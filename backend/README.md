# AutoAds Backend

## Development

1. Copy `.env.example` to `.env` and adjust values.
2. Install deps:

```sh
npm i
```

3. Start dev server:

```sh
npm run dev
```

The API will run on `http://localhost:3001` by default.

## Endpoints

- `GET /api/health`
- `POST /api/contact`
