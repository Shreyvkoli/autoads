PORT=3001
CORS_ORIGIN=http://localhost:8080

# Protect admin endpoints (set a strong random string)
ADMIN_TOKEN=123456

# Optional: if set, /api/contact will email submissions
CONTACT_TO=
SMTP_HOST=
SMTP_PORT=587
SMTP_USER=
SMTP_PASS=
SMTP_SECURE=false
